package customers.project.demo.services;

import javax.transaction.Transactional;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import customers.project.demo.entities.Configuration;

//@Service
//@Transactional
public class ConfigurationService {
/*@Autowired
	ConfigurationRepository configrepository;
	public void editConfig(Configuration config) {
		configrepository.findAll().forEach(conf->{
			conf.setChemin_projet(config.getChemin_projet());
			conf.setPort_server(config.getPort_server());
			conf.setPwd_db(config.getPwd_db());
			conf.setUrl_server(config.getUrl_server());
			conf.setUrl_db(config.getUrl_db());
			conf.setUser_db(config.getUser_db());
		});
	}*/
}
